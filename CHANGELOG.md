# 更新日志

本项目的重要版本变化记录在此。版本号由 `fpk/manifest` 统一管理。

## [1.1.0] - 2026-09-12

v1.1.0 重点完善 Mihomo Core 生命周期、网络配置可靠性和代理节点操作，并重新整理仪表盘与设置页面的信息结构。

### Core 管理与启动恢复

- 新增可持久保存的 `Manager 托管` / `外部 Mihomo` 运行方式，可在设置页明确切换，重启后继续沿用。
- Manager 托管模式支持启动、停止和重启 Core；手动停止 Core 后管理页面与设置能力继续可用。
- `all` 通用包增加明确的 Core 下载流程、实时进度、停止下载和手动上传入口；安装过程校验版本与摘要，并支持失败回滚。
- 启动前检查实际监听端口冲突，提供可直接修改端口的处理入口，不会自动停止用户已有的外部 Mihomo。
- 加强托管进程识别、PID 校验、优雅停止和早退检测，避免误操作无关进程，并给出可执行的配置路径与 Controller 错误提示。

### 网络设置与跨订阅持久化

- 托管 Core 停止时仍可读取、修改并校验网络端口；启动后自动使用已保存配置。
- 新增 TCP/UDP 端口占用与重复端口检查，网络配置按“备份、写入、校验、应用、确认”执行，失败时自动回滚。
- 将用户明确修改的端口、局域网访问、全局 IPv6、统一延迟、TUN 和 GEO 设置保存到独立配置，并在订阅更新或应用前合并。
- DNS 覆盖启用时同步保留 DNS、Fake IP、Fallback、Nameserver Policy 与 Hosts 设置，避免更新订阅后被覆盖。
- 改进自动保存队列，合并连续修改并保留保存期间产生的新值，同时显示与本次操作对应的后端进度和失败原因。

### 仪表盘与代理节点

- 重构仪表盘的当前连接、运行控制、订阅和实时状态区域，标题可直接进入对应页面，运行控制标题只进入设置首页。
- 新增代理组和节点下拉选择器，可查看节点延迟并按延迟排序；弹窗使用可拖动滚动滑块，并阻止滚动到底后带动外层页面。
- 调整标题箭头、字段对齐、字体、弹窗宽度和信息密度，完善宽屏与窄屏布局。
- 代理节点页面新增组内筛选、按延迟或名称排序、隐藏超时节点、定位当前节点及排序偏好保存。
- 订阅更新支持条件请求；内容未变化时跳过重复应用，并区分“仅更新”与“更新并应用”。

### 设置、更新与可观测性

- 重整设置页分类和 Core/DNS 子页，集中管理 Core、Controller、Secret、网络端口、TUN、GEO、系统代理和应用更新。
- 新增应用版本更新提示及开关；常规 GitHub 请求失败时可绕过 HTTP/HTTPS 代理直接重试，并选择匹配当前平台的 FPK。
- Secret 默认不通过公共设置接口返回，仅在用户明确点击查看时读取。
- 当前配置页面新增搜索、高亮、上一个/下一个匹配项和横向滚动滑块。
- 优化日志、规则、连接和仪表盘滚动体验；连接列表中的长进程名保持单行并以省略号显示。

### 安装、升级与卸载

- 新增卸载数据选择：默认保留应用配置，选择删除数据时清理配置、主目录与运行数据；重新安装时恢复已保留配置。
- 构建脚本在生成前自动从 manifest 同步前端版本，确保包名、页面、Go 二进制和包内元数据一致。
- 支持从 v1.0.0 直接覆盖升级；升级后建议检查 Core 运行方式、端口、TUN、DNS 和出口连接状态。
- 旧版本无法区分某项配置是订阅值还是用户偏好，因此需要跨订阅保留的项目请在升级后重新保存一次。

## [1.0.0] - 2026-09-09

v1.0.0 是 Clash for fnos 的首个正式大版本。该版本将运行时从 Node.js 全面迁移到 Go，并重构 Vue 前端及 fnOS 系统集成。

### 核心架构

- 使用静态 Go Web 服务替换 Node.js 服务，移除 `nodejs_v22` 运行依赖。
- 新增独立 Go Root Helper，通过受限 Unix Socket 承担必要的 root 操作。
- 将 Mihomo Controller、配置事务、订阅任务、日志、网络设置、更新与系统管理统一迁移到 Go。
- `fpk/manifest` 作为唯一版本源，构建时同步前端版本并注入 Go 二进制。
- 提供 `x86_64`、`arm64` 离线 Core 包，以及运行时下载匹配 Core 的 `all` 通用包。

### TUN 与网络

- 仪表盘增加 TUN 快速开关和实时操作进度，设置页继续提供完整参数。
- TUN 修改采用准备、配置校验、激活、Controller 健康检查、提交与失败回滚流程。
- 增加运行态轮询与托管 Core 重启兜底，处理网卡释放延迟及 Controller 切换失败。
- 升级停止阶段等待 Mihomo 正常释放 TUN；新版本启动后自动核对并恢复托管 TUN 状态。
- 默认 MTU 调整为 `1500`，增加 Stack/MTU 帮助说明。
- 支持 `route-exclude-address` IPv4/IPv6 CIDR 编辑、规范化、去重与校验。
- 完善 Mixed、HTTP、SOCKS、Redir、TProxy、Allow LAN、IPv6、DNS 劫持与严格路由设置。

### 仪表盘与运行状态

- 重构当前连接、运行控制、当前订阅及实时状态布局，改善全屏与窄窗口空间利用。
- 增加出口 IP、国家/地区、位置、时区和节点延迟展示。
- 增加跨 Core/Web 重启持久保存的累计上传与下载统计。
- 增加最近 10 分钟实时流量历史、断流重连和持久化恢复。
- 增加 Mihomo RSS 内存流式展示，并统一监听端口字体和架构标签。

### 订阅、配置与规则

- 订阅更新、应用和本地 YAML 导入改为 Go 异步任务，增加进度与错误状态。
- 支持授权目录扫描、本地配置候选识别，并显示当前托管/外部 Core 运行信息。
- 配置查看、编辑、校验、应用、启动同步、备份和失败恢复统一由 Go 编排。
- 支持代理组选择持久化、配置顺序恢复和 Rule Provider 批量/单独更新。
- GEO 数据支持状态查看、缺失项下载、立即更新和托管模式自动更新。

### 系统代理、日志与连接

- 系统代理环境变量支持 `/etc/environment`、`/etc/profile` 和 `/etc/bash.bashrc`，并可跟随 Mixed Port。
- 仅管理带明确标记的代理配置块；兼容清理旧标记，保留用户其他内容，并在启动时同步持久设置。
- 日志支持行数选择、级别筛选、搜索高亮和自动换行切换。
- 优化连接规则、Payload 与横向滚动显示，保留单连接及全部连接关闭能力。

### 界面与体验

- 使用 Vue 3、TypeScript 与 Vite 重构全部页面，统一 fnOS 桌面窗口、亮暗主题和响应式布局。
- 优化仪表盘大屏布局、卡片层级、输入框标签、展开箭头和页面密度。
- 新增 TUN Stack/MTU 帮助弹窗，点击显示并在离开弹窗区域后关闭。
- 更新面板精简为应用与 Mihomo Core 独立检查，并显示完整 `x86_64` / `arm64` 架构。
- 改进订阅任务单行状态、代理节点折叠控件、监听端口显示和全局滚动条行为。

### 升级说明

- 支持从 v0.8.x 通过 fnOS 手动安装覆盖升级。
- 升级过程中应用和托管 Mihomo 会被短暂停止；开启 TUN 时网络会短暂中断。
- 配置和运行数据继续使用 fnOS 的应用配置与数据目录；建议升级后检查 Core、TUN 和出口连接状态。
- 本地构建、自动化测试与 FPK 审计不能替代真实 fnOS 设备上的安装、升级和网络验证。

## [0.8.14] - 2026-09-07

- 修复订阅请求客户端标识，提升不同订阅服务返回 Mihomo YAML 的兼容性。
- 完成双架构与通用 FPK 发布校验。

## [0.8.13] - 2026-09-05

- 增加日志行数筛选、搜索与高亮。
- 增加系统代理环境变量开关，并统一版本号管理。

## [0.8.12] - 2026-08-30

- 修复 fnOS 应用图标切换与缓存更新。

## [0.8.5] - 2026-08-30

- 调整 Mihomo DNS 默认启用与外层 DNS 覆写默认关闭的独立语义。
- 完善 DNS 配置备份、校验、应用与回滚。

## [0.8.0] - 2026-08-29

- 发布新的设置和 DNS 管理界面。
- 增加 x86_64、ARM64 与通用安装包。
