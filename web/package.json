{
  "name": "clash-for-fnos-web",
  "version": "1.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "vue-tsc --noEmit",
    "test": "vitest run",
    "build": "npm run typecheck && vite build",
    "check": "npm run typecheck && npm test && vite build"
  },
  "dependencies": {
    "@trimjs/web-app": "0.4.2",
    "vue": "^3.5.42"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.8",
    "@types/node": "^24.10.0",
    "@vue/tsconfig": "^0.8.1",
    "typescript": "~5.9.3",
    "vite": "^8.2.2",
    "vitest": "^4.1.0",
    "vue-tsc": "^3.3.11"
  },
  "engines": {
    "node": ">=22.12"
  }
}
